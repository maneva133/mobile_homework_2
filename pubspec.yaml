name: mobile_homework_2
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

  # Firebase core (latest compatible)
  firebase_core: ^3.7.0

  # Messaging (your version 15.5.0 DOES NOT EXIST → fixed)
  firebase_messaging: ^15.2.10

  # Firestore
  cloud_firestore: ^5.6.12

  # Local notifications + timezone for scheduling
  flutter_local_notifications: ^17.2.1
  timezone: ^0.9.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true